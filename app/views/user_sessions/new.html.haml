- content_for :title, "Login to CollabCave"
.login
  .login__box
    .login__logo
      = image_tag 'collabcave_logo.svg', alt: 'CollabCave Logo'
    .login__form
      = render 'form'
    .login__register
      Don't have an account?
      = link_to "Sign Up", new_user_path
